apply plugin: 'java'

task wrapper(type: Wrapper) {
    gradleVersion = '2.10'
}

jar {
    baseName = 'Rabbit Catcher'

    manifest {
        attributes 'Main-Class': 'com.kanoonsantikul.catcher.Catcher'
    }
}
